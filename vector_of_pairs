#include<bits/stdc++.h>
using namespace std;

void printVec(vector <pair<int,int>> &v){
    for (int i = 0; i < v.size(); i++)
    {
        cout<<v[i].first<<" "<<v[i].second<<"\n";
    }    
}

int main(){
    vector <pair<int, int>> v;
    int N,x,y,n;
    cin>>N;
    for (int i = 0; i < N; i++)
    {
            cin>>x>>y;
            v.push_back({x,y});
            // or can use v.push_back(make_pair(x,y));
    }
    printVec(v);
    

    return 0;
}
